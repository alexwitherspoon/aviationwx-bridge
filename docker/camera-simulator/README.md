# Camera simulator for the E2E harness.
#
# HTTP: GET /http/{cam}/snapshot.jpg
# RTSP: rtsp://camera-simulator:8554/cam-b (mediamtx, looped seq-001.jpg)
# Control: POST /control/advance, POST /control/reset, GET /control/state
